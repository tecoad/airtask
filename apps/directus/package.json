{
  "name": "@airtask/directus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx directus start",
    "test": "jest",
    "test-seed": "ts-node scripts/test-plan-seed.ts",
    "create-snapshot": "npx directus schema snapshot ./snapshots/latest.yaml",
    "apply-snapshot": "npx directus schema apply ./snapshots/latest.yaml",
    "apply-snapshot:plan": "npx directus schema apply --dry-run ./snapshots/latest.yaml",
    "apply-snapshot:ci": "npx directus schema apply --yes ./snapshots/latest.yaml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "directus": "^10.8.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "typescript": "^4.9.4"
  }
}
