type SubscriptionPortalResult {
  url: String!
}

input SubscriptionPortalInput {
  accountId: ID!
}

extend type Query {
  subscriptionPortal(input: SubscriptionPortalInput!): SubscriptionPortalResult!
}
